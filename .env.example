# URL base del API backend
# En desarrollo, Vite proxy redirige /api al backend (ver vite.config.ts)
# En producción, apuntar al dominio real
VITE_API_URL=https://api-stagging.pos-atienda.com/api/v1

# Entorno
VITE_APP_ENV=development